name: Build Tauri Artifacts

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build-tauri:
    runs-on: ubuntu-24.04
    container:
      image: linuxdeepin/deepin:25
      options: --user root --privileged

    env:
      GIT_CONFIG_COUNT: "1"
      GIT_CONFIG_KEY_0: "safe.directory"
      GIT_CONFIG_VALUE_0: "*"
      # 设置显示环境，避免 GTK 相关问题
      DISPLAY: ":99"
      GDK_BACKEND: "x11"

    steps:
      - name: Install Git (Required for submodules)
        run: |
          sudo apt-get update
          sudo apt-get install -y git
          git --version

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: 0

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libfuse2 \
            libssl-dev \
            libgtk-3-dev \
            libxdo-dev \
            pkg-config \
            build-essential \
            curl wget git unzip file binutils xvfb xauth
            
      - name: Setup virtual display
        run: |
          Xvfb :99 -screen 0 1024x768x24 > /dev/null 2>&1 &
          echo "Virtual display started"

      - name: Install Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install pnpm
        run: |
          corepack enable
          corepack prepare pnpm@9.13.2 --activate
          pnpm --version

      - name: Install Rust toolchain
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          export PATH="$HOME/.cargo/bin:$PATH"
          rustup show active-toolchain

      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "^2" --locked

      - name: Install frontend dependencies
        run: pnpm install

      - name: Download required resources
        shell: bash -e {0}      # 原来是 sh -e {0}
        run: |
          set -e
          
          # 创建资源目录
          mkdir -p src-tauri/resources
          
          # 定义资源文件URL
          GEOIP_URL="https://github.com/Dreamacro/maxmind-geoip/releases/latest/download/Country.mmdb"
          GEOSITE_URL="https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geosite.dat"
          GEOIP_DAT_URL="https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geoip.dat"
          
          # 下载资源文件
          echo "=== 正在下载资源文件 ==="
          
          # Country.mmdb
          if [ ! -f "src-tauri/resources/Country.mmdb" ]; then
            echo "下载 Country.mmdb..."
            curl -L -o src-tauri/resources/Country.mmdb "$GEOIP_URL"
            echo "✅ Country.mmdb 下载完成: $(stat -c%s src-tauri/resources/Country.mmdb) bytes"
          else
            echo "ℹ️ Country.mmdb 已存在，跳过下载"
          fi
          
          # geosite.dat
          if [ ! -f "src-tauri/resources/geosite.dat" ]; then
            echo "下载 geosite.dat..."
            curl -L -o src-tauri/resources/geosite.dat "$GEOSITE_URL"
            echo "✅ geosite.dat 下载完成: $(stat -c%s src-tauri/resources/geosite.dat) bytes"
          else
            echo "ℹ️ geosite.dat 已存在，跳过下载"
          fi
          
          # geoip.dat
          if [ ! -f "src-tauri/resources/geoip.dat" ]; then
            echo "下载 geoip.dat..."
            curl -L -o src-tauri/resources/geoip.dat "$GEOIP_DAT_URL"
            echo "✅ geoip.dat 下载完成: $(stat -c%s src-tauri/resources/geoip.dat) bytes"
          else
            echo "ℹ️ geoip.dat 已存在，跳过下载"
          fi
          
          # 验证资源文件
          echo ""
          echo "=== 资源文件验证 ==="
          REQUIRED_FILES=(
            "src-tauri/resources/Country.mmdb"
            "src-tauri/resources/geoip.dat"
            "src-tauri/resources/geosite.dat"
          )
          
          ALL_FILES_VALID=true
          for file in "${REQUIRED_FILES[@]}"; do
            if [ ! -f "$file" ]; then
              echo "❌ 缺少必要资源文件: $file"
              ALL_FILES_VALID=false
            elif [ $(stat -c%s "$file") -eq 0 ]; then
              echo "❌ 资源文件为空: $file"
              ALL_FILES_VALID=false
            else
              echo "✅ 资源文件有效: $file ($(stat -c%s "$file") bytes)"
            fi
          done
          
          if [ "$ALL_FILES_VALID" = "false" ]; then
            echo ""
            echo "❌ 资源文件验证失败，请检查网络连接或手动下载"
            exit 1
          fi
          
          echo ""
          echo "=== 所有资源文件验证通过 ==="        

      - name: Download sidecars and resources (prebuild)
        run: |
          set -e
          node scripts/prebuild.mjs x86_64-unknown-linux-gnu
          echo "=== src-tauri/sidecar/ ==="
          ls -la src-tauri/sidecar/
          echo "=== src-tauri/resources/ ==="
          ls -la src-tauri/resources/ 2>/dev/null || true

      - name: Verify required resources
        shell: bash -e {0}      # 原来是 sh -e {0}
        run: |
          set -e
          # 检查必要的资源文件
          REQUIRED_FILES=(
            "src-tauri/resources/Country.mmdb"
            "src-tauri/resources/geoip.dat"
            "src-tauri/resources/geosite.dat"
          )
          
          for file in "${REQUIRED_FILES[@]}"; do
            if [ ! -f "$file" ]; then
              echo "❌ 缺少必要资源文件: $file"
              echo "尝试重新下载..."
              
              # 根据文件名重新下载
              case "$file" in
                *Country.mmdb)
                  curl -L -o "$file" "https://github.com/Dreamacro/maxmind-geoip/releases/latest/download/Country.mmdb"
                  ;;
                *geosite.dat)
                  curl -L -o "$file" "https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geosite.dat"
                  ;;
                *geoip.dat)
                  curl -L -o "$file" "https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geoip.dat"
                  ;;
              esac
              
              # 再次验证
              if [ -f "$file" ] && [ $(stat -c%s "$file") -gt 0 ]; then
                echo "✅ 重新下载成功: $file"
              else
                echo "❌ 重新下载失败: $file"
                exit 1
              fi
            else
              echo "✅ 资源文件存在: $file ($(stat -c%s "$file") bytes)"
            fi
          done
          
          echo ""
          echo "=== 所有资源文件验证通过 ==="

      - name: Build Tauri application (no bundle)
        run: |
          cargo tauri build --no-bundle --verbose
          echo "=== Build output ==="
          ls -la target/release/
          file target/release/clash-verge

      - name: Generate service install script
        run: |
          set -e
          # 创建服务安装脚本
          cat > target/release/clash-verge-service-install << 'EOF'
          #!/bin/bash
          # Clash Verge Service 安装脚本（玲珑环境适配版）
          set -e
          
          APP_DIR="/opt/apps/io.github.clash-verge-rev/files"
          SERVICE_NAME="clash-verge-service"
          USER_HOME="${HOME:-/home/user}"
          INSTALL_DIR="${USER_HOME}/.local/share/io.github.clash-verge-rev.clash-verge-rev"
          
          # 创建必要的目录
          mkdir -p "${INSTALL_DIR}"
          mkdir -p "${USER_HOME}/.config/systemd/user/"
          
          # 创建 systemd 用户服务
          cat > "${USER_HOME}/.config/systemd/user/${SERVICE_NAME}.service" << UNIT
          [Unit]
          Description=Clash Verge Service
          After=network.target
          
          [Service]
          Type=simple
          ExecStart=${APP_DIR}/clash-verge-service
          Restart=on-failure
          RestartSec=5
          User=${USER}
          
          [Install]
          WantedBy=default.target
          UNIT
          
          # 重新加载 systemd
          systemctl --user daemon-reload || true
          systemctl --user enable ${SERVICE_NAME} || true
          
          echo "✅ Clash Verge Service 安装完成"
          EOF
          chmod +x target/release/clash-verge-service-install

      - name: Prepare linglong-project directory
        run: |
          set -e
          mkdir -p linglong-project
          
          # 复制主程序
          cp target/release/clash-verge linglong-project/clash-verge
          chmod +x linglong-project/clash-verge
          
          # 复制服务安装脚本
          cp target/release/clash-verge-service-install linglong-project/clash-verge-service-install
          chmod +x linglong-project/clash-verge-service-install
          
          # 复制 sidecar
          cp -r src-tauri/sidecar linglong-project/sidecar 2>/dev/null || true
          
          # 复制资源文件（确保包含所有必要文件）
          if [ -d "src-tauri/resources" ]; then
            cp -r src-tauri/resources linglong-project/resources
            echo "=== 资源文件列表 ==="
            find linglong-project/resources -type f
          fi
          
          # 复制图标
          if [ -d "src-tauri/icons" ]; then
            cp -r src-tauri/icons linglong-project/icons
          fi

          # 创建 AppRun.sh 启动脚本
          cat > linglong-project/AppRun.sh << 'EOF'
          #!/bin/bash
          set -e
          
          # 设置基本环境
          export WEBKIT_DISABLE_COMPOSITING_MODE=1
          export WEBKIT_DISABLE_SANDBOX_THIS_IS_DANGEROUS=1
          export GIO_LAUNCHED_DESKTOP_FILE_PID=$$           
          # 设置图形环境
          export GDK_BACKEND=x11
          export DISPLAY=${DISPLAY:-:0}
          export XAUTHORITY=${XAUTHORITY:-/home/user/.Xauthority}
          
          # 设置库路径
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export PATH="$DIR/sidecar:$PATH"
          export LD_LIBRARY_PATH="$DIR/lib:$DIR/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH"
          
          # 设置用户数据目录
          export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
          export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
          export XDG_CACHE_HOME="${XDG_CACHE_HOME:-$HOME/.cache}"
          
          # 创建必要的用户目录
          APP_DATA_DIR="$XDG_DATA_HOME/io.github.clash-verge-rev.clash-verge-rev"
          mkdir -p "$APP_DATA_DIR"
          mkdir -p "$XDG_CONFIG_HOME/io.github.clash-verge-rev.clash-verge-rev"
          mkdir -p "$XDG_CACHE_HOME/io.github.clash-verge-rev.clash-verge-rev"
          
          # 复制资源文件到用户数据目录（如果不存在）
          if [ -d "$DIR/resources" ]; then
            for resource in Country.mmdb geoip.dat geosite.dat; do
              if [ -f "$DIR/resources/$resource" ] && [ ! -f "$APP_DATA_DIR/$resource" ]; then
                cp "$DIR/resources/$resource" "$APP_DATA_DIR/"
                echo "已复制资源文件: $resource"
              fi
            done
          fi
          
          # 设置 sidecar 权限
          chmod +x "$DIR/sidecar"/* 2>/dev/null || true
          
          # 处理 systemd 服务（在容器内可能不可用）
          if ! systemctl --user is-active clash-verge-service &>/dev/null; then
            echo "警告: systemd 服务不可用，将使用内置模式运行"
            export CLASH_VERGE_NO_SERVICE=1
          fi
          
          # 启动应用
          exec "$DIR/clash-verge" "$@"
          EOF
          chmod +x linglong-project/AppRun.sh

          # 创建 desktop 文件
          mkdir -p linglong-project/share/applications
          mkdir -p linglong-project/share/icons/hicolor/256x256/apps
          cat > linglong-project/share/applications/io.github.clash-verge-rev.desktop << 'EOF'
          [Desktop Entry]
          Name=Clash Verge Rev
          Comment=Clash Meta GUI
          Exec=/opt/apps/io.github.clash-verge-rev/files/AppRun.sh
          Icon=io.github.clash-verge-rev
          Type=Application
          Categories=Network;
          Terminal=false
          StartupNotify=true
          EOF

          # 复制图标
          if [ -f linglong-project/icons/128x128.png ]; then
            cp linglong-project/icons/128x128.png linglong-project/share/icons/hicolor/256x256/apps/io.github.clash-verge-rev.png
          elif [ -f linglong-project/icons/icon.png ]; then
            cp linglong-project/icons/icon.png linglong-project/share/icons/hicolor/256x256/apps/io.github.clash-verge-rev.png
          fi

          echo "=== linglong-project 目录结构 ==="
          find linglong-project -type f | sort

      - name: Upload linglong-project artifact
        uses: actions/upload-artifact@v4
        with:
          name: linglong-project
          path: linglong-project/
          if-no-files-found: error
          retention-days: 30
